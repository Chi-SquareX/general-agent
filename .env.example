# Copy this file to `.env` in the same directory and set your key.
# Docker Compose loads `.env` automatically for variable substitution.
# Bun also loads `.env` when you run `bun run agent-server` locally.

ANTHROPIC_API_KEY=
